current json is incomplete. But when fenders fail then it reports Cabin not load correctly? 
    Make error more meaningful.

# Documentation #
Call "document.init(" + json + ");");
>To run in browser:
//Global model
		var DATA = {};

		DATA = HIMEJI;

At end init.js

?AUTOSTART needed in url
